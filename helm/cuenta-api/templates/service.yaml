apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-cuenta.fullname" . }}
  labels:
    {{- include "api-cuenta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "api-cuenta.selectorLabels" . | nindent 4 }}
